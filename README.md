# Telegram Join Tracker Bot

## How to Run
1. Replace `YOUR_BOT_TOKEN_HERE` with your bot token.
2. Replace `@your_channel_here` with your channel ID.
3. Run `pip install -r requirements.txt`
4. Run the bot: `python3 bot.py`
